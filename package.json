{
  "name": "planets",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "dev:api": "node scripts/dev-api.mjs"
  },
  "dependencies": {
    "@vercel/og": "^0.6.8",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
